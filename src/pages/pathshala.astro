---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import HeroSection from '../components/HeroSection.astro';
import Carousel from '../components/Carousel.astro';
import TestimonialCard from '../components/TestimonialCard.astro';
import ProgramCard from '../components/ProgramCard.astro';
import StatsGrid from '../components/StatsGrid.astro';
import { 
  hero, 
  carouselImages, 
  educationalApproach, 
  testimonials, 
  programs, 
  impactStats, 
  cta 
} from '../data/pathshalaData';
import '../styles/global.css';
import { generateMetaTags, generatePageSchema, ORGANIZATION_SCHEMA } from '../data/seoConstants';

const seoData = generateMetaTags("pathshala");
const pageSchema = generatePageSchema("pathshala");
---

<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<meta name="generator" content={Astro.generator} />
		
		<!-- Primary Meta Tags -->
		<title>{seoData.title}</title>
		<meta name="title" content={seoData.title} />
		<meta name="description" content={seoData.description} />
		<meta name="keywords" content={seoData.keywords} />
		<meta name="author" content="Koshish Family" />
		<meta name="robots" content="index, follow" />
		<link rel="canonical" href={seoData.canonical} />
		
		<!-- Open Graph / Facebook -->
		<meta property="og:type" content={seoData.openGraph.type} />
		<meta property="og:url" content={seoData.openGraph.url} />
		<meta property="og:title" content={seoData.openGraph.title} />
		<meta property="og:description" content={seoData.openGraph.description} />
		<meta property="og:image" content={seoData.openGraph.image} />
		<meta property="og:site_name" content={seoData.openGraph.siteName} />
		<meta property="og:locale" content={seoData.openGraph.locale} />
		
		<!-- Twitter -->
		<meta name="twitter:card" content={seoData.twitter.card} />
		<meta name="twitter:url" content={seoData.openGraph.url} />
		<meta name="twitter:title" content={seoData.twitter.title} />
		<meta name="twitter:description" content={seoData.twitter.description} />
		<meta name="twitter:image" content={seoData.twitter.image} />
		<meta name="twitter:site" content={seoData.twitter.site} />
		<meta name="twitter:creator" content={seoData.twitter.creator} />
		
		<!-- Additional Meta Tags -->
		<meta name="theme-color" content="#0F52BA" />
		<meta name="apple-mobile-web-app-capable" content="yes" />
		<meta name="apple-mobile-web-app-status-bar-style" content="default" />
		<meta name="format-detection" content="telephone=no" />
		
		<!-- External Resources -->
		<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
		
		<!-- Structured Data -->
		<script type="application/ld+json" set:html={JSON.stringify(ORGANIZATION_SCHEMA)} />
		<script type="application/ld+json" set:html={JSON.stringify(pageSchema)} />
	</head>
	<body class="text-black font-inter">
		<!-- Skip to main content link for accessibility -->
		<a href="#main-content" class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 bg-primary text-white px-4 py-2 rounded z-50">Skip to main content</a>
		<Header />
		
		<main id="main-content">
			<!-- Hero Section - Education is Empowerment Style -->
			<section class="relative bg-linear-to-r from-blue-600 to-green-500 text-white min-h-[70vh] flex items-center justify-center">
				<div class="absolute inset-0">
					<img src={hero.backgroundImage} alt="Education Background" class="w-full h-full object-cover opacity-30" />
				</div>
				<div class="relative z-10 text-center px-4">
					<h1 class="text-4xl md:text-6xl font-bold mb-4 font-jost tracking-wide">
						EDUCATION IS EMPOWERMENT
					</h1>
					<p class="text-xl md:text-2xl font-light">
						{hero.subtitle}
					</p>
				</div>
			</section>

			<div class="container mx-auto px-4 py-16">
				<!-- Why Education Section -->
				<section class="mb-20">
					<h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-8 font-jost">
						WHY EDUCATION
					</h2>
					<p class="text-lg text-gray-700 leading-relaxed max-w-4xl">
						If we need to address healthcare, poverty, population control, unemployment and human 
						rights, there's no better way to start than providing education to children in need. Education 
						not only empowers children to have a secure future but also helps them grow up as 
						responsible national and global citizens. The Right to Education (RTE) Act which came into 
						force in 2010 made education free and compulsory for all children in the age group of 6-14 
						years. But even a decade later, the learning curve has not been steady for many children in 
						the country. The socio-economic conditions of parents and lack of proper learning in schools 
						are hindrances which prevent many children from having education.
					</p>
				</section>

				<!-- What We Do Section -->
				<section class="mb-20">
					<h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-8 font-jost">
						WHAT WE DO
					</h2>
					<p class="text-lg text-gray-700 leading-relaxed max-w-4xl mb-12">
						Koshish Family's flagship programme Pathshala works with the objective of 
						empowering underprivileged children by providing education, nutrition, and wellness 
						opportunities for all. Pathshala works with children (3-18 years) living in difficult 
						circumstances, children from poor families, differently abled children, disaster-struck 
						children, abandoned and street children, and children living in tribal belts, remote villages 
						and hard to reach areas.
					</p>

					<!-- Program Flow Chart -->
					<div class="bg-gray-50 rounded-2xl p-8 mb-12">
						<div class="flex flex-wrap justify-center items-center gap-8 sm:gap-20">
							{programs.map((program, index) => (
								<div class="flex flex-col items-center text-center max-w-xs">
									<div class="w-24 h-24 bg-green-100 rounded-full flex items-center justify-center mb-4 border-4 border-green-300">
										<i class={`${program.icon} text-3xl text-green-600`}></i>
									</div>
									<h3 class="font-bold text-2xl text-gray-800 mb-2 font-jost">{program.title}</h3>
									<p class="text-gray-600">{program.description}</p>
									{index < programs.length - 1 && (
										<div class="hidden md:block absolute right-0 top-1/2 transform -translate-y-1/2">
											<i class="fas fa-arrow-right text-green-400 text-2xl"></i>
										</div>
									)}
								</div>
							))}
						</div>
					</div>
				</section>

				<!-- 4-Pronged Approach -->
				<section class="mb-20">
					<h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-12 font-jost">
						4-PRONGED APPROACH
					</h2>
					<div class="grid md:grid-cols-2 gap-6">
						{educationalApproach.focusAreas.map((area, index) => {
							const colors = ['bg-yellow-100 text-yellow-800', 'bg-green-100 text-green-800', 'bg-purple-100 text-purple-800', 'bg-blue-100 text-blue-800'];
							return (
								<div class={`${colors[index]} p-6 rounded-lg flex items-center`}>
									<i class={`${area.icon} text-2xl mr-4`}></i>
									<span class="font-semibold text-lg">{area.text}</span>
								</div>
							);
						})}
					</div>
				</section>

				<!-- Impact 2023-24 -->
				<section class="mb-20">
					<h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-12 font-jost">
						IMPACT 2023-24
					</h2>
					<div class="grid md:grid-cols-2 gap-12 items-center">
						<div class="space-y-8">
							{impactStats.map((stat, index) => (
								<div class="flex items-center space-x-6">
									<div class="text-4xl md:text-6xl font-bold text-green-500">
										{stat.value}
									</div>
									<div>
										<p class="text-lg font-semibold text-gray-800">{stat.label}</p>
									</div>
								</div>
							))}
						</div>
						<div class="grid grid-cols-2 gap-4">
							{educationalApproach.images.map((img, index) => (
								<img 
									src={img} 
									alt={`Student ${index + 1}`}
									class="rounded-lg shadow-lg object-cover w-full h-48"
									loading="lazy"
								/>
							))}
						</div>
					</div>
				</section>

				<!-- Carousel Gallery -->
				<section id="carousel-section" class="mb-20">
					<div class="bg-green-50 rounded-2xl p-8">
						<h2 class="text-3xl font-bold text-gray-800 mb-6 text-center font-jost">
							Our Journey in Pictures
						</h2>
						<Carousel images={carouselImages} />
					</div>
				</section>

				<!-- Stories of Change -->
				<section class="mb-20">
					<h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-12 font-jost">
						STORIES OF CHANGE
					</h2>
					<div class="grid md:grid-cols-3 gap-8">
						{testimonials.map((testimonial, index) => (
							<div class="bg-green-50 rounded-lg p-6">
								<div class="flex items-center mb-4">
									<img 
										src={testimonial.image} 
										alt={testimonial.author}
										class="w-16 h-16 rounded-full object-cover mr-4"
										loading="lazy"
									/>
									<div>
										<h3 class="font-bold text-gray-800 font-jost">{testimonial.author}</h3>
									</div>
								</div>
								<p class="text-gray-700 italic">"{testimonial.quote}"</p>
							</div>
						))}
					</div>
				</section>

				<!-- Change That Inspires Us -->
				<section class="mb-20">
					<h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-12 text-center font-jost">
						CHANGE THAT INSPIRES US
					</h2>
					<div class="grid md:grid-cols-2 gap-8">
						<div class="bg-linear-to-br from-blue-50 to-green-50 rounded-xl p-8">
							<h3 class="text-2xl font-bold text-gray-800 mb-4 font-jost">Our Mission</h3>
							<p class="text-gray-700 mb-6">
								{educationalApproach.description}
							</p>
							<div class="space-y-3">
								{educationalApproach.focusAreas.slice(0, 2).map(area => (
									<div class="flex items-center">
										<i class={`${area.icon} text-blue-500 mr-3`}></i>
										<span class="text-gray-700">{area.text}</span>
									</div>
								))}
							</div>
						</div>
						<div class="bg-linear-to-br from-green-50 to-yellow-50 rounded-xl p-8">
							<h3 class="text-2xl font-bold text-gray-800 mb-4 font-jost">Join Our Community</h3>
							<p class="text-gray-700 mb-6">
								{cta.description}
							</p>
							<a
								href={cta.buttonLink}
								target="_blank"
								rel="noopener noreferrer"
								class="inline-block bg-blue-600 text-white px-8 py-3 rounded-full font-semibold hover:bg-blue-700 transition-all duration-300"
							>
								{cta.buttonText}
							</a>
						</div>
					</div>
				</section>
			</div>
		</main>

		<Footer />
	</body>
</html>
